<Window x:Class="QuickPanel.Settings.AddAppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:QuickPanel.Services"
        Title="QuickPanel"
        Width="420" Height="540"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Border CornerRadius="20" Background="{DynamicResource Md3Surface}"
            BorderBrush="{DynamicResource Md3Outline}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Opacity="0.55" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{loc:Loc AddApp_Title}" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource Md3OnSurface}" Margin="0,0,0,4"/>
            <TextBlock Grid.Row="1" Text="{loc:Loc AddApp_Subtitle}"
                       FontSize="12" Foreground="{DynamicResource Md3OnSurfaceVariant}" Margin="0,0,0,16"/>

            <!-- Presets -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="PresetList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- Custom -->
            <StackPanel Grid.Row="3" Margin="0,16,0,0">
                <TextBlock Text="{loc:Loc AddApp_Customize}" FontSize="11"
                           Foreground="{DynamicResource Md3OnSurfaceVariant}" Margin="0,0,0,8"/>
                <TextBox x:Name="InNombre" Style="{StaticResource Md3TextBox}"
                         Tag="Nombre" Margin="0,0,0,8"/>
                <TextBox x:Name="InUrl" Style="{StaticResource Md3TextBox}"
                         Tag="https://..."/>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="{loc:Loc Common_Cancel}" Click="Cancel_Click" Style="{StaticResource Md3TextButton}" Margin="0,0,8,0"/>
                <Button Content="{loc:Loc Common_Add}" Click="Add_Click" Style="{StaticResource Md3FilledButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
